[build]
builder = "nixpacks"
buildCommand = "NODE_OPTIONS='--max-old-space-size=512' npm run build"

[deploy]
startCommand = "npx next start -p ${PORT:-3000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
